# DataScrapeSpotifyAPI

basically we will be using python3 to:
  1) get the access token
  2) fetch playlist items information
  3) fetch other search items info, like track and album, etc (simple search not user based)
